[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers
markers =
    slow: tests that take more than a second
    network: tests that would touch the network (always skipped in CI)
filterwarnings =
    ignore::DeprecationWarning
